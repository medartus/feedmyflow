# whitelist
branches:
    only:
      - master
      - dev

language: node_js
node_js:
    - node
script:
    - cd functions
    - npm run test
    - cd ..
    - cd website
    - npm run test
# deploy:
#     provider: npm
#     email: "$NPM_EMAIL"
#     api_key: "$NPM_TOKEN"
# notifications:
#     email: true