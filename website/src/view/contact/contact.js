import React from "react";
import Header from "../../components/header/header";

const Contact = (props) => {
  return (
    <>
      <Header {...props} />
    </>
  );
};

export default Contact;
