import React from "react";
import Header from "../../components/header/header";

const About = (props) => {
  return (
    <>
      <Header {...props} />
    </>
  );
};

export default About;
